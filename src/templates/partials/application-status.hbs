Статус:
{{#if status.pending}}
	<span class="pending">на рассмотрении</span>
	{{else if status.approved}}
	<span class="approved">одобрено</span>
	{{else if status.rejected}}
	<span class="rejected">отказано</span>
	{{else if status.resolved}}
	<span class="resolved">закрыто</span>
{{else}}
	<span class="resolved">неизвестен</span>
{{/if}}

{{#if status.resolver}}
	<br>
	<span class="resolver">Вердикт вынес:
		<a class="{{status.resolver.role}}" href="/user/{{status.resolver.userslug}}">{{status.resolver.username}}</a>
	</span>
{{/if}}
